import { PageShell } from "@/components/layout/page-shell";

export default function CalendarPage() {
  return (
    <PageShell
      eyebrow="Calendar"
      title="訓練月曆"
      description="此頁後續會顯示每日訓練、完成狀態、休息日、高強度日與計畫版本切換。"
    />
  );
}
