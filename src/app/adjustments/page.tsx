import { PageShell } from "@/components/layout/page-shell";

export default function AdjustmentsPage() {
  return (
    <PageShell
      eyebrow="Replan"
      title="計畫調整"
      description="此頁後續會顯示 AI 調整原因、受影響日期、調整前後比較與確認套用流程。"
    />
  );
}
